{
  "name": "@eternal-pond/worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "tsc -p tsconfig.json && wrangler types --check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local --port 8787",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "dependencies": {
    "@eternal-pond/shared": "2.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.7",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.113.0"
  }
}
